/****** IMLTEXT_EN.H  ********** Belenkov O.V.  *****  18/04/16 ****
*                                                                  *
*              Copyright (c) PANORAMA Group 1991-2016              *
*                      All Rights Reserved                         *
*                                                                  *
********************************************************************
*                                                                  *
*                      СТРОКОВЫЕ КОНСТАНТЫ                         *
*                                                                  *
*******************************************************************/

// Формирование переменной константы (постфикс указывает на язык)
#define VCONST(_y) _y ## EN

//------------------------------------------------------------------
// АНГЛИЙСКИЙ ИНТЕРФЕЙС (EN)
//------------------------------------------------------------------
WCHAR VCONST(imlmain1)[]    = WTEXT("Orient mark");                              // 1
WCHAR VCONST(imlmain2)[]    = WTEXT("Mark parameters");                          // 2
WCHAR VCONST(imlmain3)[]    = WTEXT("Lights");                                   // 3
WCHAR VCONST(imlmain4)[]    = WTEXT("Mark border");                              // 4
WCHAR VCONST(imlmain5)[]    = WTEXT("Centre mark");                              // 5
WCHAR VCONST(imlmain6)[]    = WTEXT("Mark parameters");                          // 6
WCHAR VCONST(imlmain7)[]    = WTEXT("Semantic number for angle in degree");      // 7
WCHAR VCONST(imlmain8)[]    = WTEXT("Horizontal shift (mm on the map)");         // 8
WCHAR VCONST(imlmain9)[]    = WTEXT("Vertical shift (mm on the map)");           // 9
WCHAR VCONST(imlmain10)[]   = WTEXT("Restrictions mark");                        // 10
WCHAR VCONST(imlmain11)[]   = WTEXT("Default mark parameters");                  // 11
WCHAR VCONST(imlmain12)[]   = WTEXT("Mark depends on the semantics");            // 12
WCHAR VCONST(imlmain13)[]   = WTEXT("Default mark parameters");                  // 13
WCHAR VCONST(imlmain14)[]   = WTEXT("Semantic number");                          // 14
WCHAR VCONST(imlmain15)[]   = WTEXT("Minimum");                                  // 15
WCHAR VCONST(imlmain16)[]   = WTEXT("Maximum");                                  // 16
WCHAR VCONST(imlmain17)[]   = WTEXT("First mark parameters");                    // 17
WCHAR VCONST(imlmain18)[]   = WTEXT("Second mark parameters");                   // 18
WCHAR VCONST(imlmain19)[]   = WTEXT("Third mark parameters");                    // 19
WCHAR VCONST(imlmain20)[]   = WTEXT("Fourth mark parameters");                   // 20
WCHAR VCONST(imlmain21)[]   = WTEXT("Depth polygon");                            // 21
WCHAR VCONST(imlmain22)[]   = WTEXT("Object code");                              // 22
WCHAR VCONST(imlmain23)[]   = WTEXT("Obstrn");                                   // 23
WCHAR VCONST(imlmain24)[]   = WTEXT("Object local");                             // 24
WCHAR VCONST(imlmain25)[]   = WTEXT("Object code");                              // 25
WCHAR VCONST(imlmain26)[]   = WTEXT("Mark and text with different language");    // 26
WCHAR VCONST(imlmain27)[]   = WTEXT("Mark parameters");                          // 27
WCHAR VCONST(imlmain28)[]   = WTEXT("Depth contour");                            // 28
WCHAR VCONST(imlmain29)[]   = WTEXT("Код объекта");                              // 29
WCHAR VCONST(imlmain30)[]   = WTEXT("Depth curve");                              // 30
WCHAR VCONST(imlmain31)[]   = WTEXT("Object code");                              // 31
WCHAR VCONST(imlmain32)[]   = WTEXT("Shoreline construction");                   // 32
WCHAR VCONST(imlmain33)[]   = WTEXT("Object local");                             // 33
WCHAR VCONST(imlmain34)[]   = WTEXT("Default mark parameters");                  // 34
WCHAR VCONST(imlmain35)[]   = WTEXT("Wrecks");                                   // 35
WCHAR VCONST(imlmain36)[]   = WTEXT("Object local");                             // 36
WCHAR VCONST(imlmain37)[]   = WTEXT("Object code");                              // 37
WCHAR VCONST(imlmain38)[]   = WTEXT("Cable text");                               // 38
WCHAR VCONST(imlmain39)[]   = WTEXT("Low accuracy line");                        // 39
WCHAR VCONST(imlmain40)[]   = WTEXT("Sounds");                                   // 40

#include "imltext.h"
