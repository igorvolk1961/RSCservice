/****** IMLTEXT_RU.H  ********** Belenkov O.V.  *****  18/04/16 ****
*                                                                  *
*              Copyright (c) PANORAMA Group 1991-2016              *
*                      All Rights Reserved                         *
*                                                                  *
********************************************************************
*                                                                  *
*                      СТРОКОВЫЕ КОНСТАНТЫ                         *
*                                                                  *
*******************************************************************/

// Формирование переменной константы
#define VCONST(_y) _y ## RU

//------------------------------------------------------------------
// РУССКИЙ ИНТЕРФЕЙС (RU)
//------------------------------------------------------------------
WCHAR VCONST(imlmain1)[]    = WTEXT("Ориентированный знак");                     // 1
WCHAR VCONST(imlmain2)[]    = WTEXT("Параметры знака");                          // 2
WCHAR VCONST(imlmain3)[]    = WTEXT("Огни");                                     // 3
WCHAR VCONST(imlmain4)[]    = WTEXT("Габариты знака");                           // 4
WCHAR VCONST(imlmain5)[]    = WTEXT("Центрированный знак");                      // 5
WCHAR VCONST(imlmain6)[]    = WTEXT("Параметры знака");                          // 6
WCHAR VCONST(imlmain7)[]    = WTEXT("Номер семантики - угол в градусах");        // 7
WCHAR VCONST(imlmain8)[]    = WTEXT("Горизонтальное смещение (мм на карте)");    // 8
WCHAR VCONST(imlmain9)[]    = WTEXT("Вертикальное смещение (мм на карте)");      // 9
WCHAR VCONST(imlmain10)[]   = WTEXT("Знак по ограничениям");                     // 10
WCHAR VCONST(imlmain11)[]   = WTEXT("Параметры знака по умолчанию");             // 11
WCHAR VCONST(imlmain12)[]   = WTEXT("Знак по семантике");                        // 12
WCHAR VCONST(imlmain13)[]   = WTEXT("Параметры знака по умолчанию");             // 13
WCHAR VCONST(imlmain14)[]   = WTEXT("Номер семантики");                          // 14
WCHAR VCONST(imlmain15)[]   = WTEXT("Минимум");                                  // 15
WCHAR VCONST(imlmain16)[]   = WTEXT("Максимум");                                 // 16
WCHAR VCONST(imlmain17)[]   = WTEXT("Параметры первого знака");                  // 17
WCHAR VCONST(imlmain18)[]   = WTEXT("Параметры второго знака");                  // 18
WCHAR VCONST(imlmain19)[]   = WTEXT("Параметры третьего знака");                 // 19
WCHAR VCONST(imlmain20)[]   = WTEXT("Параметры четвертого знака");               // 20
WCHAR VCONST(imlmain21)[]   = WTEXT("Изображение площадных глубин");             // 21
WCHAR VCONST(imlmain22)[]   = WTEXT("Код объекта");                              // 22
WCHAR VCONST(imlmain23)[]   = WTEXT("Изображение препятствий");                  // 23
WCHAR VCONST(imlmain24)[]   = WTEXT("Локализация объекта");                      // 24
WCHAR VCONST(imlmain25)[]   = WTEXT("Код объекта");                              // 25
WCHAR VCONST(imlmain26)[]   = WTEXT("Знак со сменой языка");                     // 26
WCHAR VCONST(imlmain27)[]   = WTEXT("Параметры знака");                          // 27
WCHAR VCONST(imlmain28)[]   = WTEXT("Контур глубины");                           // 28
WCHAR VCONST(imlmain29)[]   = WTEXT("Код объекта");                              // 29
WCHAR VCONST(imlmain30)[]   = WTEXT("Изобата");                                  // 30
WCHAR VCONST(imlmain31)[]   = WTEXT("Код объекта");                              // 31
WCHAR VCONST(imlmain32)[]   = WTEXT("Береговые конструкции");                    // 32
WCHAR VCONST(imlmain33)[]   = WTEXT("Локализация объекта");                      // 33
WCHAR VCONST(imlmain34)[]   = WTEXT("Параметры знака по умолчанию");             // 34
WCHAR VCONST(imlmain35)[]   = WTEXT("Вид остатков кораблекрушения");             // 35
WCHAR VCONST(imlmain36)[]   = WTEXT("Локализация объекта");                      // 36
WCHAR VCONST(imlmain37)[]   = WTEXT("Код объекта");                              // 37
WCHAR VCONST(imlmain38)[]   = WTEXT("Подпись к кабелю");                         // 38
WCHAR VCONST(imlmain39)[]   = WTEXT("Линия низкой точности");                    // 39
WCHAR VCONST(imlmain40)[]   = WTEXT("Отметки глубин");                           // 40

#include "imltext.h"
